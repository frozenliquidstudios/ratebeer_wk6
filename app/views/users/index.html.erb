<p id="notice"><%= notice %></p>

<h1>Listing Users</h1>

<table class="table">
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= link_to user.username, user, class:'btn btn-primary' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

